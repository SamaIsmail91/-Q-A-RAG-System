# For Streamlit Community Cloud: copy this file's contents into your app's
# "Secrets" panel (Settings -> Secrets) instead of committing a real key.
# For local development, you can also save this as .streamlit/secrets.toml
# (it's gitignored) — the app will pick it up automatically via st.secrets.

ANTHROPIC_API_KEY = "sk-ant-your-key-here"
