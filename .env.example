# Copy this file to .env and fill in your key, OR paste the key directly
# into the app's sidebar at runtime, OR set it as a Streamlit Cloud secret
# (see README.md "Deployment" section). The app checks all three, in that
# order of priority: sidebar input > st.secrets > this environment variable.

ANTHROPIC_API_KEY=sk-ant-your-key-here
